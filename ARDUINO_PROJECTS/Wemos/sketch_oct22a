int pin = 2;

void setup() {
  // Переключаем контакт GPIO2 в режим вывода данных (OUTPUT):
  pinMode(pin, OUTPUT);
}

void loop() {
  digitalWrite(pin, HIGH);   
                             
  delay(1000);               
  digitalWrite(pin, LOW);    
                             
  delay(50);               
}
